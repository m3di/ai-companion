node_modules/
data/
.env
*.log
dist/
.idea/

# Instance-specific knowledge & data — this is a general framework repo; each
# deployment keeps its own knowledge locally / on its PVC, never in git.
knowledge-map.md
kb/*.pl
